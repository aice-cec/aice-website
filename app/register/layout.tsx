import { ReactNode } from "react";

export const metadata = {
  title: "Register | AICE CEC",
  description: "Join the AI Innovation Community for Excellence at College of Engineering Chengannur.",
};

export default function RegisterLayout({
  children,
}: {
  children: ReactNode;
}) {
  return <>{children}</>;
}
